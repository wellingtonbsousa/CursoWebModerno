// Código Não Executável

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}
objeto.processamento() // O objeto funciona como uma cápsula. O foco passou a ser o objeto

// Principios importantes
// 1. Abstração -> traduzir o mundo real para o objeto
// 2. Encapsulamento -> mudando o objeto, quem usa não precisa se adaptar
// 3. Herança (Prototype) -> reutilização de código (ex: carro.motor)
// 4. Polimorfismo ->

